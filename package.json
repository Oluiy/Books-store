{
  "name": "books-store",
  "private": true,
  "version": "1.0.0",
  "description": "Books Store monorepo (backend + Frontend)",
  "scripts": {
    "start": "node backend/index.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false && cd Frontend && npm install && npm install vite@4 && npm run build  && cd ../backend && npm install"
  },
  "engines": {
    "node": "20.x"
  }
}
