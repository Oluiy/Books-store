{
  "name": "books-store",
  "private": true,
  "version": "1.0.0",
  "description": "Books Store monorepo (backend + Frontend)",
  "scripts": {
    "start": "node backend/index.js",
    "heroku-postbuild": "npm install --prefix backend && NPM_CONFIG_PRODUCTION=false npm install --prefix Frontend && ROLLUP_USE_NODE_JS_IMPLEMENTATION=true npm run build --prefix Frontend"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
